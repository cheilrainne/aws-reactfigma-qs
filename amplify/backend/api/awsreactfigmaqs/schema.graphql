type Registration @model @auth(rules: [{allow: public}]) {
  id: ID!
  fullname: String
  email: AWSEmail
  mobile: AWSPhone
  invoiceNo: String
  invoiceAmount: Float
  receiptImage: String
  entry: Int
  entryTotal: Int
} 